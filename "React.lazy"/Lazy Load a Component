import React, { Suspense } from 'react'
const About = React.lazy(()=>"./About ");

const App = () => {
  return (
    <div>
      <Suspense fallback={<div>Loading...</div>}>
        <About/>
      </Suspense>
    </div>
  )
}

export default App
